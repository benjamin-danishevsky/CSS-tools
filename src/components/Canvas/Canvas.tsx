export default function Canvas() {
  return (
    <section
      className="flex flex-1 items-center justify-center"
      style={{ backgroundColor: "var(--color-canvas-bg)" }}
    >
      <p style={{ color: "var(--color-text-muted)" }}>Grid canvas (Phase 2)</p>
    </section>
  );
}
